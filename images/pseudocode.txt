ENTRADA: Instancia de problema (1000 empleados, 999 empleados, ... 25 empleados, 24 empleados!)
SALIDA: Solución al problema (% productividad, tiempo computacional)

solucion = (vacía)
Servicios = ordenar servicios originales por fecha/hora
for (servicio : Servicios) {
    empleado = seleccionar empleado aleatorio
    asignar servicio a empleado (empleado está a tope ese día)
}
evaluar solución

devolver solución

=============================
mejorSolución = (vacía)
for (i = 1 : 200) {
    solucion = (vacía)
    for (servicio : Servicios) {
        empleado = seleccionar empleado aleatorio
        asignar servicio a empleado
    }
    evaluar solución
    if (solución es mejor que mejorSolución) {
        mejorSolución = solución
    }
}

devolver mejorSolución



=============================

solucion = vacía
for (servicio : Servicio) {
    asignar servicio a alguno de los empleados en esta franja de tiempo (cumpliendo con las restricciones de tiempo máximo, no solapamiento, etc.)
    if (no hay empleado) {
        ampliar el número de empleados en 1 unidad (con 8 horas de trabajo) y asignar
    }
}
evaluar solucion -> % productividad + número de empleados + jornada de trabajo [hora comienzo, hora fin]
